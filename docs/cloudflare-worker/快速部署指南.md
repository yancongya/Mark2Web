# 🚀 小米 Mimo 代理 - 3分钟快速部署指南

## ⚡ 超快速部署（推荐）

### **方式 1: Cloudflare Dashboard（无需安装任何工具）**

#### **第1步: 登录 Cloudflare**
打开浏览器 → 访问 [https://dash.cloudflare.com](https://dash.cloudflare.com)
如果没有账号，注册一个（完全免费）

#### **第2步: 创建 Worker**
```
左侧菜单 → Workers & Pages → Create Application → Create Worker
```

#### **第3步: 复制粘贴代码**
1. 给 Worker 命名：`xiaomimimo-v2-flashxy`
2. 点击 **Deploy**
3. 点击 **Edit Code**
4. 删除所有默认代码
5. 打开 `worker.js` 文件，全选复制
6. 粘贴到 Cloudflare 编辑器
7. 点击 **Save and Deploy**

#### **第4步: 获取 URL**
部署完成后，你会看到：
```
✅ 部署成功！
URL: https://xiaomimimo-v2-flashxy.your-subdomain.workers.dev
```
**复制这个 URL！**

---

### **方式 2: 命令行部署（适合开发者）**

```bash
# 1. 安装 Wrangler
npm install -g wrangler

# 2. 登录
wrangler login

# 3. 进入目录
cd cloudflare-worker

# 4. 部署
wrangler deploy

# 5. 完成！会自动显示 URL
```

---

## 📋 **Mark2Web 配置**

部署成功后，在 Mark2Web 中配置：

### **设置 → 模型服务商 → 添加小米 Mimo**

```
Provider ID: xiaomi-mimo
Type: openai
Label: 小米 Mimo
Base URL: https://api.xiaomimimo.com
Proxy URL: https://xiaomimimo-v2-flashxy.your-subdomain.workers.dev  ← 你的 Worker URL
API Key: 你的小米 Mimo API Key
Model ID: mimo-v2-flash
```

---

## ✅ **验证部署**

### **快速测试**

访问你的 Worker URL：
```
https://xiaomimimo-v2-flashxy.your-subdomain.workers.dev
```

应该看到错误信息（正常，因为没有 POST 数据），说明 Worker 正在运行！

### **完整测试**

```bash
# 替换为你的 URL 和 API Key
curl -X POST https://xiaomimimo-v2-flashxy.your-subdomain.workers.dev \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"mimo-v2-flash","messages":[{"role":"user","content":"你好"}]}'
```

---

## 🎯 **在 Mark2Web 中使用**

1. **打开 Mark2Web**
2. **进入设置** → **模型服务商**
3. **配置小米 Mimo**（使用上面的配置）
4. **测试连接** → 应该显示 ✅ 连接成功
5. **生成代码** → 现在可以正常使用了！

---

## 🔍 **常见问题**

### **Q: 部署后无法访问？**
A: 检查：
- Worker 是否已部署（不是保存但未部署）
- URL 是否正确
- 网络是否正常

### **Q: 测试连接失败？**
A: 检查：
- Proxy URL 是否填写正确
- API Key 是否有效
- Worker 日志：`wrangler tail`

### **Q: 想删除 Worker？**
A: Cloudflare Dashboard → Workers & Pages → 选择 Worker → Settings → Delete

---

## 💰 **费用说明**

**完全免费！**
- Cloudflare Workers 免费额度：100,000 请求/天
- 个人使用完全足够

---

## 📞 **需要帮助？**

1. **查看日志**：
   ```bash
   wrangler tail
   ```

2. **测试 Worker**：
   ```bash
   curl -X POST https://your-worker.workers.dev \
     -H "Authorization: Bearer test" \
     -d '{"model":"test","messages":[]}'
   ```

3. **检查状态**：https://www.cloudflarestatus.com/

---

## 🎉 **完成！**

部署成功后，你的 Mark2Web 就可以在浏览器中直接使用小米 Mimo 了！

**现在就开始部署吧！** 🚀
