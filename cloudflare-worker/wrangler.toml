# Cloudflare Worker 配置文件
# 使用方法: wrangler deploy

name = "xiaomimimo-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# 可选: 自定义域名
# route = "proxy.yourdomain.com/*"

# 可选: 环境变量（如果需要）
# [vars]
# ENVIRONMENT = "production"
